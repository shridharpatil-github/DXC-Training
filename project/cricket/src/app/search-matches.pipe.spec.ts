import { SearchMatchesPipe } from './search-matches.pipe';

describe('SearchMatchesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMatchesPipe();
    expect(pipe).toBeTruthy();
  });
});
